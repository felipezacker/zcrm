# AIOS Environment Report
# Generated by @devops (Gage) — *environment-bootstrap
generated_at: "2026-02-07T21:20:00Z"
project_name: zcrm
project_type: brownfield

system:
  os: Darwin
  architecture: arm64
  platform: macOS
  user: felipezacker
  package_manager: none (Homebrew not installed)

cli_tools:
  essential:
    git:
      installed: true
      version: "2.50.1"
      source: "Apple Git-155"
    gh:
      installed: true
      version: "2.86.0"
      authenticated: true
      account: felipezacker
      protocol: ssh
      scopes: "admin:public_key, gist, read:org, repo"
    node:
      installed: true
      version: "24.13.0"
    npm:
      installed: true
      version: "11.6.2"

  infrastructure:
    supabase:
      installed: false
      available_via_npx: true
      npx_version: "2.76.3"
      note: "Use 'npx supabase' instead of global install"
    railway:
      installed: false
    docker:
      installed: false
      note: "Download Docker Desktop from https://www.docker.com/products/docker-desktop/"

  quality:
    coderabbit:
      installed: false
      note: "Install via: curl -fsSL https://coderabbit.ai/install.sh | bash"

  optional:
    pnpm:
      installed: false
      available_via_npx: true
      note: "Use 'npx pnpm' or 'corepack enable && corepack prepare pnpm@latest --activate'"
    bun:
      installed: true
      version: "1.3.8"

repository:
  initialized: true
  remote_url: "https://github.com/felipezacker/zcrm.git"
  branch: aios
  has_gitignore: true
  has_package_json: true

validation:
  essential_complete: true
  recommended_complete: false
  ready_for_development: true

pending_installations:
  - tool: homebrew
    command: '/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"'
    note: "Requires sudo — run manually in terminal"
  - tool: docker
    command: "brew install --cask docker"
    alternative: "Download from https://www.docker.com/products/docker-desktop/"
  - tool: coderabbit
    command: "curl -fsSL https://coderabbit.ai/install.sh | bash"
